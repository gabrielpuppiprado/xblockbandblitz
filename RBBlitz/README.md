# Precisa extrair as partes e mesclar o conteudo dentro do xBlockBandBlitz (Content)

## No linux só usar o unrar para extrair (sudo apt install rar unrar && unrar x RBBlitz_Demo.part1.rar);
## No windows é só extrair a primeira parte (usando o winrar) com as outras partes na mesma pasta;
